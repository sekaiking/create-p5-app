function setup() {
  const canvaSize = min(windowWidth, windowHeight);
  createCanvas(canvaSize, canvaSize);
  noLoop();
  background("#010110");
  fill(255)
  textSize(24);
  text("edit src/sketch.js to get started", 100, 100)
}
function draw() {
}
